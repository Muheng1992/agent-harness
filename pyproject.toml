[project]
name = "agent-harness"
version = "0.1.0"
description = "Autonomous Agent Harness — Claude Code CLI based multi-agent orchestration system"
readme = "README.md"
requires-python = ">=3.10"
license = {text = "MIT"}

[project.scripts]
agent-ctl = "agent_ctl:main"
