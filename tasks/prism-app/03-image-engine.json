{
  "id": "image-engine",
  "project": "prism-app",
  "project_dir": "/Users/muhengli/dev/Prism",
  "goal": "建立 Prism 的核心影像處理引擎——Data 層的 ImageProcessingRepository 實作。\n\n## 檔案：Prism/Data/Services/ImageProcessingService.swift\n\n使用 Core Image 實作所有影像處理功能。這是整個 App 最核心的元件。\n\n### 主要功能\n\n1. **applyFilter** — 套用濾鏡預設\n   - 將 FilterParameters 轉換為 CIFilter 鏈\n   - 支援 intensity（0~1）控制濾鏡強度（用原圖和濾鏡結果做 alpha blending）\n   - 核心 CIFilter：\n     - `CIColorControls`（brightness, contrast, saturation）\n     - `CITemperatureAndTint`（color temperature）\n     - `CIExposureAdjust`（exposure）\n     - `CIHighlightShadowAdjust`（highlights, shadows）\n     - `CIUnsharpMask`（sharpen）\n     - `CIVignette`（vignette）\n     - `CIGaussianBlur` + blend（fade 效果）\n\n2. **applyAdjustments** — 套用手動調整\n   - 與 applyFilter 類似，但用 AdjustmentParams\n   - 額外支援：\n     - HSL 調整（用 `CIHueAdjust` 或 Metal kernel）\n     - Grain（用 `CIRandomGenerator` + `CIBlendWithMask`）\n     - Clarity（用 unsharp mask + 特定 radius）\n     - Skin Tone（用 color matrix 針對膚色範圍調整）\n     - Split Tone（分別對 highlight/shadow 上色）\n\n3. **applyCrop** — 裁切和變形\n   - `CICrop` + `CIAffineTransform`（旋轉）\n   - `CIPerspectiveCorrection`（透視校正）\n\n4. **generateThumbnail** — 縮圖生成\n   - `CILanczosScaleTransform` 生成高品質縮圖\n\n### CIContext 管理\n\n```swift\nfinal class ImageProcessingService: ImageProcessingRepository, @unchecked Sendable {\n    private let context: CIContext\n    \n    init() {\n        // 使用 Metal 加速\n        if let device = MTLCreateSystemDefaultDevice() {\n            self.context = CIContext(mtlDevice: device)\n        } else {\n            self.context = CIContext()\n        }\n    }\n    \n    // 內部 helper：CIImage → Data\n    private func render(_ image: CIImage, format: CIFormat = .RGBA8) -> Data? {\n        guard let cgImage = context.createCGImage(image, from: image.extent) else { return nil }\n        let uiImage = UIImage(cgImage: cgImage)\n        return uiImage.jpegData(compressionQuality: 0.95)\n    }\n}\n```\n\n### Filter Chain Builder\n\n```swift\nprivate func buildFilterChain(from params: FilterParameters, input: CIImage) -> CIImage {\n    var output = input\n    \n    // Exposure\n    if params.exposure != 0 {\n        let filter = CIFilter(name: \"CIExposureAdjust\")!\n        filter.setValue(output, forKey: kCIInputImageKey)\n        filter.setValue(params.exposure, forKey: kCIInputEVKey)\n        output = filter.outputImage ?? output\n    }\n    \n    // Color Controls (brightness, contrast, saturation)\n    let colorControls = CIFilter(name: \"CIColorControls\")!\n    colorControls.setValue(output, forKey: kCIInputImageKey)\n    colorControls.setValue(params.brightness, forKey: kCIInputBrightnessKey)\n    colorControls.setValue(params.contrast, forKey: kCIInputContrastKey)\n    colorControls.setValue(params.saturation, forKey: kCIInputSaturationKey)\n    output = colorControls.outputImage ?? output\n    \n    // Temperature and Tint\n    // ... 用 CITemperatureAndTint\n    \n    // Highlights and Shadows\n    // ... 用 CIHighlightShadowAdjust\n    \n    // Sharpen\n    // ... 用 CIUnsharpMask\n    \n    // Vignette\n    // ... 用 CIVignette\n    \n    // Grain（隨機噪點 blend）\n    // ... 用 CIRandomGenerator + CISourceOverCompositing\n    \n    // Fade（提升暗部）\n    // ... 用 CIColorMatrix 或 tone curve\n    \n    return output\n}\n```\n\n## 檔案：Prism/Data/Repositories/ImageProcessingRepositoryImpl.swift\n\n將 ImageProcessingService 包裝為 Repository 實作。\n\n## 重要\n\n- 所有 CIFilter 名稱要正確（查文件）\n- 先讀取 Domain 層的 Protocol 和 Entity 定義再實作\n- UIImage 在 Data 層可以使用（import UIKit）\n- 影像處理要在背景執行緒（actor 或 Task.detached）\n- 專案必須能 build 成功",
  "touches": ["Prism/Data/Services/ImageProcessingService.swift", "Prism/Data/Repositories/ImageProcessingRepositoryImpl.swift"],
  "verify": "xcodebuild -project Prism.xcodeproj -scheme Prism -destination 'platform=iOS Simulator,name=iPhone 17 Pro' build 2>&1 | tail -3 | grep -q 'BUILD SUCCEEDED' && echo 'PASS'",
  "depends_on": ["setup-project"],
  "max_attempts": 5
}
