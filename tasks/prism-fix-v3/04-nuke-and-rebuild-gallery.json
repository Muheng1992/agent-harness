{
  "id": "nuke-and-rebuild-gallery",
  "project": "prism-fix-v3",
  "project_dir": "/Users/muhengli/dev/Prism",
  "goal": "完全重寫 Gallery 和 PhotoDetail 的佈局——對照 Apple Photos App 的標準。\n\n## 先讀取 docs/design-spec.md\n\n## GalleryView.swift\n\nApple Photos 風格的照片網格：\n- 3 欄正方形網格，間距 1pt（不是 2pt）\n- 每張圖片用 `.fill` 填滿 cell 不留白\n- 滾動時 NavigationBar 半透明\n- 底部 padding 讓最後一行不被 TabBar 遮住\n\n```swift\nvar body: some View {\n    NavigationStack(path: $navigationPath) {\n        ScrollView {\n            LazyVGrid(columns: columns, spacing: 1) {\n                ForEach(viewModel.photos) { photo in\n                    PhotoGridItemView(photo: photo, viewModel: viewModel)\n                        .aspectRatio(1, contentMode: .fill)\n                        .clipped()\n                        .contentShape(Rectangle())\n                        .onTapGesture {\n                            navigationPath.append(photo.id)\n                        }\n                }\n            }\n        }\n        .background(PrismColors.background)\n        // ... navigation config\n    }\n}\n```\n\n## PhotoGridItemView.swift\n\n**這是最常見的錯誤**——image 必須用 `.fill` 填滿 cell：\n\n```swift\nstruct PhotoGridItemView: View {\n    let photo: Photo\n    let viewModel: GalleryViewModel\n    @State private var thumbnail: UIImage?\n    \n    var body: some View {\n        GeometryReader { geo in\n            if let image = thumbnail {\n                Image(uiImage: image)\n                    .resizable()\n                    .aspectRatio(contentMode: .fill)  // 不是 .fit！\n                    .frame(width: geo.size.width, height: geo.size.height)\n                    .clipped()\n            } else {\n                Rectangle()\n                    .fill(PrismColors.surface)\n                    .overlay {\n                        ProgressView()\n                            .tint(PrismColors.secondary)\n                            .scaleEffect(0.7)\n                    }\n            }\n        }\n        .aspectRatio(1, contentMode: .fit)\n        .task { await loadThumbnail() }\n    }\n}\n```\n\n## PhotoDetailView.swift\n\n全螢幕照片預覽（像 Apple Photos 的全螢幕模式）：\n\n```swift\nvar body: some View {\n    ZStack {\n        // 純黑背景\n        Color.black.ignoresSafeArea()\n        \n        // 照片（可縮放）\n        if let image = displayImage {\n            Image(uiImage: image)\n                .resizable()\n                .aspectRatio(contentMode: .fit)\n                .frame(maxWidth: .infinity, maxHeight: .infinity)\n                .scaleEffect(scale)\n                .offset(offset)\n                .gesture(magnificationGesture)\n                .gesture(dragGesture)\n                .onTapGesture(count: 2) { toggleZoom() }\n                .onTapGesture { toggleControls() }\n        }\n        \n        // 控制元件（可隱藏）\n        if showControls {\n            VStack {\n                // 頂部：返回 + 更多\n                HStack {\n                    Button { dismiss() } label: {\n                        Image(systemName: \"chevron.left\")\n                            .font(.title3)\n                            .foregroundStyle(.white)\n                    }\n                    Spacer()\n                }\n                .padding(.horizontal, 16)\n                .padding(.top, 8)\n                \n                Spacer()\n                \n                // 底部：編輯操作\n                HStack(spacing: 32) {\n                    actionButton(icon: \"slider.horizontal.3\", label: \"編輯\")\n                    actionButton(icon: \"camera.filters\", label: \"濾鏡\")\n                    actionButton(icon: \"crop\", label: \"裁切\")\n                    actionButton(icon: \"square.and.arrow.up\", label: \"分享\")\n                    actionButton(icon: \"trash\", label: \"刪除\")\n                }\n                .padding(.bottom, 16)\n            }\n            .transition(.opacity)\n        }\n    }\n    .navigationBarHidden(true)\n    .statusBarHidden(!showControls)\n}\n```\n\n讀取所有現有檔案再重寫。專案必須 build 成功。",
  "touches": ["Prism/Presentation/Gallery/GalleryView.swift", "Prism/Presentation/Gallery/PhotoGridItemView.swift", "Prism/Presentation/Gallery/PhotoDetailView.swift"],
  "verify": "xcodebuild -project Prism.xcodeproj -scheme Prism -destination 'platform=iOS Simulator,name=iPhone 17 Pro' build 2>&1 | tail -3 | grep -q 'BUILD SUCCEEDED' && grep -q 'contentMode: .fill' Prism/Presentation/Gallery/PhotoGridItemView.swift && echo 'PASS'",
  "depends_on": ["research-apple-hig"],
  "max_attempts": 8
}
