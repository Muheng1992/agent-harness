{
  "id": "fix-editor-layout",
  "project": "prism-fix-v2",
  "project_dir": "/Users/muhengli/dev/Prism",
  "goal": "修復照片編輯器佈局——預覽圖要佔螢幕 60-70%，工具列在底部，整體排版專業。\n\n## 問題\n\n1. PhotoEditorView 的預覽圖沒有正確填滿上方空間\n2. HSL/SplitTone 編輯器使用固定高度（260/220），不適應不同螢幕\n3. ToolPickerView 工具圖標用固定寬度 56，在不同螢幕不均勻\n4. AdjustmentSliderView 缺少零點標記和數值顯示\n5. 缺少背景色，某些狀態下與內容混合\n\n## 修復方案\n\n### PhotoEditorView.swift — 標準三層佈局\n\nVSCO/Lightroom 標準模式：\n```swift\nvar body: some View {\n    GeometryReader { geo in\n        VStack(spacing: 0) {\n            // 頂部工具列\n            topBar\n                .frame(height: 44)\n                .background(PrismColors.background)\n            \n            // 預覽區（佔 60% 高度）\n            imagePreview\n                .frame(height: geo.size.height * 0.6)\n                .clipped()\n                .background(Color.black)\n            \n            // 調整滑桿（如果有選中工具）\n            if currentTool != nil {\n                adjustmentSlider\n                    .frame(height: 60)\n                    .transition(.move(edge: .bottom).combined(with: .opacity))\n            }\n            \n            // 工具選擇列\n            toolPicker\n                .frame(height: geo.size.height * 0.15)\n        }\n    }\n    .background(PrismColors.background)\n    .ignoresSafeArea(edges: .bottom)\n}\n```\n\n### AdjustmentSliderView.swift — VSCO 風格滑桿\n\n```swift\nstruct AdjustmentSliderView: View {\n    @Binding var value: Double\n    let range: ClosedRange<Double>\n    let isCentered: Bool  // 是否有零點（如 brightness: -100~100）\n    \n    var body: some View {\n        GeometryReader { geo in\n            ZStack {\n                // 軌道\n                RoundedRectangle(cornerRadius: 1)\n                    .fill(PrismColors.secondary.opacity(0.3))\n                    .frame(height: 2)\n                \n                // 零點標記（如果 isCentered）\n                if isCentered {\n                    Rectangle()\n                        .fill(PrismColors.secondary)\n                        .frame(width: 2, height: 12)\n                        .position(x: geo.size.width / 2, y: geo.size.height / 2)\n                }\n                \n                // 填充條\n                filledTrack(in: geo)\n                \n                // Thumb\n                Circle()\n                    .fill(.white)\n                    .frame(width: 20, height: 20)\n                    .shadow(color: .black.opacity(0.3), radius: 2)\n                    .position(x: thumbPosition(in: geo), y: geo.size.height / 2)\n                    .gesture(dragGesture(in: geo))\n            }\n        }\n        .frame(height: 40)\n        .overlay(alignment: .top) {\n            // 數值顯示\n            Text(\"\\(Int(value))\")\n                .font(.caption2)\n                .foregroundStyle(.white)\n        }\n    }\n}\n```\n\n### ToolPickerView.swift — 響應式工具列\n\n- 工具圖標寬度用 `max(48, geo.size.width / CGFloat(tools.count + 2))`\n- 已調整的工具下方顯示白色小圓點\n- 選中的工具名稱變白色\n\n### HSLEditorView.swift — 相對高度\n\n色相環大小用 `min(120, geo.size.width * 0.3)`\n\n讀取所有相關檔案後再修改。所有 View 必須有正確背景色。專案必須 build 成功。",
  "touches": ["Prism/Presentation/Editor/PhotoEditorView.swift", "Prism/Presentation/Editor/AdjustmentSliderView.swift", "Prism/Presentation/Editor/ToolPickerView.swift", "Prism/Presentation/Editor/HSLEditorView.swift"],
  "verify": "xcodebuild -project Prism.xcodeproj -scheme Prism -destination 'platform=iOS Simulator,name=iPhone 17 Pro' build 2>&1 | tail -3 | grep -q 'BUILD SUCCEEDED' && grep -q 'GeometryReader' Prism/Presentation/Editor/PhotoEditorView.swift && echo 'PASS'",
  "depends_on": [],
  "max_attempts": 5
}
