{
  "id": "fix-camera-fullscreen",
  "project": "prism-fix-v2",
  "project_dir": "/Users/muhengli/dev/Prism",
  "goal": "修復相機預覽——必須真正全螢幕、滿版顯示。這是最嚴重的 UI 問題。\n\n## 問題分析\n\nCameraPreviewView 使用 UIViewRepresentable 包裝 AVCaptureVideoPreviewLayer，但：\n1. PreviewLayer 的 frame 沒有在 layoutSubviews 中正確更新\n2. 沒有使用自訂 UIView 子類來確保 layer frame 跟隨 bounds\n3. SwiftUI 層級缺少正確的 frame modifier\n\n## 修復方案\n\n### CameraPreviewView.swift 完全重寫\n\n```swift\nimport SwiftUI\nimport AVFoundation\n\nstruct CameraPreviewView: UIViewRepresentable {\n    let session: AVCaptureSession\n    \n    func makeUIView(context: Context) -> PreviewUIView {\n        let view = PreviewUIView()\n        view.previewLayer.session = session\n        view.previewLayer.videoGravity = .resizeAspectFill\n        view.backgroundColor = .black\n        return view\n    }\n    \n    func updateUIView(_ uiView: PreviewUIView, context: Context) {\n        uiView.previewLayer.session = session\n    }\n}\n\n// 自訂 UIView 子類，確保 previewLayer 的 frame 永遠跟隨 bounds\nclass PreviewUIView: UIView {\n    override class var layerClass: AnyClass {\n        AVCaptureVideoPreviewLayer.self\n    }\n    \n    var previewLayer: AVCaptureVideoPreviewLayer {\n        layer as! AVCaptureVideoPreviewLayer\n    }\n}\n```\n\n**關鍵技巧**：使用 `layerClass` override 而非手動管理 sublayer。這樣 previewLayer 就是 view 的根 layer，frame 永遠等於 bounds，不需要手動同步。\n\n### CameraView.swift 修復佈局\n\n相機預覽必須是最底層的全螢幕背景：\n\n```swift\nvar body: some View {\n    ZStack {\n        // 底層：全螢幕相機預覽\n        CameraPreviewView(session: viewModel.cameraSession)\n            .ignoresSafeArea(.all)\n        \n        // 中間層：UI 控制疊加（用 VStack 分佈）\n        VStack(spacing: 0) {\n            // 頂部工具列（半透明背景）\n            topToolbar\n                .background(.ultraThinMaterial.opacity(0.3))\n            \n            Spacer()\n            \n            // 底部：濾鏡選擇 + 拍照按鈕 + 模式切換\n            VStack(spacing: 12) {\n                filterStrip\n                captureControls\n                modeSelector\n            }\n            .padding(.bottom, 16)\n            .background(\n                LinearGradient(\n                    colors: [.clear, .black.opacity(0.6)],\n                    startPoint: .top,\n                    endPoint: .bottom\n                )\n            )\n        }\n    }\n    .statusBarHidden(true)\n}\n```\n\n### 模擬器 Placeholder\n\n模擬器上沒有相機，必須顯示美觀的 placeholder：\n```swift\n#if targetEnvironment(simulator)\nZStack {\n    Color.black.ignoresSafeArea()\n    VStack(spacing: 16) {\n        Image(systemName: \"camera.fill\")\n            .font(.system(size: 64))\n            .foregroundStyle(.gray)\n        Text(\"相機預覽\")\n            .font(.title2)\n            .foregroundStyle(.gray)\n        Text(\"請在實機上使用相機功能\")\n            .font(.caption)\n            .foregroundStyle(.gray.opacity(0.7))\n    }\n}\n#else\nCameraPreviewView(session: viewModel.cameraSession)\n    .ignoresSafeArea(.all)\n#endif\n```\n\n## 同時修復\n\n- CameraControlsView.swift — 手動控制面板使用相對高度而非固定 320\n- FocusIndicatorView.swift — 確保對焦框在正確位置\n- 確保整個 CameraView 的 ZStack 佈局是從邊緣到邊緣\n\n讀取所有相關檔案後再修改。專案必須 build 成功。",
  "touches": ["Prism/Data/Services/CameraPreviewView.swift", "Prism/Presentation/Camera/CameraView.swift", "Prism/Presentation/Camera/CameraControlsView.swift", "Prism/Presentation/Camera/FocusIndicatorView.swift"],
  "verify": "xcodebuild -project Prism.xcodeproj -scheme Prism -destination 'platform=iOS Simulator,name=iPhone 17 Pro' build 2>&1 | tail -3 | grep -q 'BUILD SUCCEEDED' && grep -q 'ignoresSafeArea' Prism/Presentation/Camera/CameraView.swift && grep -q 'layerClass\\|PreviewUIView' Prism/Data/Services/CameraPreviewView.swift && echo 'PASS'",
  "depends_on": [],
  "max_attempts": 5
}
