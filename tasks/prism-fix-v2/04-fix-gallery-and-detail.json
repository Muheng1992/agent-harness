{
  "id": "fix-gallery-and-detail",
  "project": "prism-fix-v2",
  "project_dir": "/Users/muhengli/dev/Prism",
  "goal": "修復照片庫和詳細檢視的佈局問題。\n\n## 問題\n\n1. GalleryView 的 LazyVGrid 可能被導航列遮擋\n2. PhotoDetailView 的圖片沒有填滿空間\n3. 底部操作列沒有延伸到 Home Indicator\n4. 空狀態圖標在大螢幕太小\n5. PhotoGridItemView 缺少正確的 aspectRatio\n\n## 修復方案\n\n### GalleryView.swift\n\n```swift\nvar body: some View {\n    NavigationStack {\n        ZStack {\n            PrismColors.background.ignoresSafeArea()\n            \n            if photos.isEmpty {\n                emptyState\n            } else {\n                ScrollView {\n                    LazyVGrid(columns: columns, spacing: 2) {\n                        ForEach(photos) { photo in\n                            PhotoGridItemView(photo: photo)\n                                .aspectRatio(1, contentMode: .fill)\n                                .clipped()\n                                .onTapGesture { selectedPhoto = photo }\n                        }\n                    }\n                }\n                .scrollIndicators(.hidden)\n            }\n        }\n        .navigationTitle(\"照片庫\")\n        .navigationBarTitleDisplayMode(.inline)\n        .toolbar { ... }\n        .toolbarBackground(PrismColors.background, for: .navigationBar)\n        .toolbarColorScheme(.dark, for: .navigationBar)\n    }\n}\n```\n\n### PhotoGridItemView.swift\n\n每個 cell 必須是正方形且填滿：\n```swift\nstruct PhotoGridItemView: View {\n    let photo: Photo\n    var body: some View {\n        GeometryReader { geo in\n            // 圖片\n            Image(uiImage: thumbnail)\n                .resizable()\n                .aspectRatio(contentMode: .fill)\n                .frame(width: geo.size.width, height: geo.size.height)\n                .clipped()\n        }\n        .aspectRatio(1, contentMode: .fit)\n    }\n}\n```\n\n### PhotoDetailView.swift\n\n全螢幕照片預覽：\n```swift\nvar body: some View {\n    ZStack {\n        Color.black.ignoresSafeArea()\n        \n        // 照片（可縮放）\n        imageContent\n            .frame(maxWidth: .infinity, maxHeight: .infinity)\n        \n        // 底部操作列\n        VStack {\n            Spacer()\n            actionBar\n                .padding(.horizontal, 20)\n                .padding(.bottom, 8)\n                .background(\n                    LinearGradient(\n                        colors: [.clear, .black.opacity(0.7)],\n                        startPoint: .top,\n                        endPoint: .bottom\n                    )\n                    .ignoresSafeArea(edges: .bottom)\n                )\n        }\n    }\n    .navigationBarBackButtonHidden(true)\n    .toolbar {\n        ToolbarItem(placement: .topBarLeading) {\n            Button { dismiss() } label: {\n                Image(systemName: \"chevron.left\")\n                    .foregroundStyle(.white)\n            }\n        }\n    }\n    .toolbarBackground(.hidden, for: .navigationBar)\n}\n```\n\n讀取所有相關檔案後再修改。專案必須 build 成功。",
  "touches": ["Prism/Presentation/Gallery/GalleryView.swift", "Prism/Presentation/Gallery/PhotoDetailView.swift", "Prism/Presentation/Gallery/PhotoGridItemView.swift"],
  "verify": "xcodebuild -project Prism.xcodeproj -scheme Prism -destination 'platform=iOS Simulator,name=iPhone 17 Pro' build 2>&1 | tail -3 | grep -q 'BUILD SUCCEEDED' && grep -q 'ignoresSafeArea\\|aspectRatio' Prism/Presentation/Gallery/PhotoDetailView.swift && echo 'PASS'",
  "depends_on": [],
  "max_attempts": 5
}
