{
  "id": "ui-tests",
  "project": "prism-fix-v2",
  "project_dir": "/Users/muhengli/dev/Prism",
  "goal": "建立完整的 UI 測試——確保每個畫面都正確渲染、功能可操作。\n\n## 測試框架\n\n使用 XCTest UI Testing（XCUIApplication）。需要建立 PrismUITests target。\n\n### 步驟 1: 建立 UI Test Target\n\n如果不存在 PrismUITests，需要：\n1. 在 Prism.xcodeproj 中新增 UI Testing Bundle target\n2. 或者如果已有 PrismTests，在那裡加 UI 測試\n\n最簡單的方法：檢查現有的 project 結構，看是否已有 UITest target。如果沒有，用 xcodebuild 可能無法直接新增 target。作為替代，在 PrismTests 中使用 Swift Testing 做元件快照測試。\n\n### 步驟 2: 建立 UI 測試\n\n#### PrismUITests/LaunchTests.swift\n```swift\nimport XCTest\n\nfinal class PrismLaunchTests: XCTestCase {\n    let app = XCUIApplication()\n    \n    override func setUpWithError() throws {\n        continueAfterFailure = false\n        app.launch()\n    }\n    \n    func testAppLaunches() {\n        XCTAssertTrue(app.tabBars.firstMatch.exists)\n    }\n    \n    func testTabBarHasThreeTabs() {\n        let tabBar = app.tabBars.firstMatch\n        XCTAssertTrue(tabBar.buttons.count >= 3)\n    }\n    \n    func testCameraTabIsDefault() {\n        // 預設應該在相機 tab\n        XCTAssertTrue(app.tabBars.buttons[\"相機\"].isSelected || app.tabBars.buttons.element(boundBy: 0).isSelected)\n    }\n    \n    func testSwitchToGalleryTab() {\n        app.tabBars.buttons[\"照片庫\"].tap()\n        // Gallery 應該顯示（可能是空狀態或有照片）\n        let exists = app.staticTexts.matching(NSPredicate(format: \"label CONTAINS '照片' OR label CONTAINS '匯入'\")).firstMatch.waitForExistence(timeout: 5)\n        XCTAssertTrue(exists || app.collectionViews.firstMatch.exists || app.scrollViews.firstMatch.exists)\n    }\n    \n    func testSwitchToSettingsTab() {\n        app.tabBars.buttons[\"設定\"].tap()\n        let exists = app.staticTexts.matching(NSPredicate(format: \"label CONTAINS '設定' OR label CONTAINS 'Prism'\")).firstMatch.waitForExistence(timeout: 5)\n        XCTAssertTrue(exists || app.tables.firstMatch.exists)\n    }\n}\n```\n\n#### PrismUITests/CameraUITests.swift\n```swift\nfinal class CameraUITests: XCTestCase {\n    let app = XCUIApplication()\n    \n    override func setUpWithError() throws {\n        continueAfterFailure = false\n        app.launch()\n    }\n    \n    func testCameraViewHasControls() {\n        // 模擬器上應該顯示 placeholder\n        // 檢查是否有拍照按鈕或相機相關 UI\n        let cameraElements = app.buttons.allElementsBoundByIndex\n        XCTAssertTrue(cameraElements.count >= 1, \"Camera view should have at least one button\")\n    }\n}\n```\n\n#### PrismUITests/GalleryUITests.swift\n```swift\nfinal class GalleryUITests: XCTestCase {\n    let app = XCUIApplication()\n    \n    override func setUpWithError() throws {\n        continueAfterFailure = false\n        app.launch()\n        app.tabBars.buttons.element(boundBy: 1).tap()\n    }\n    \n    func testGalleryShowsEmptyStateOrPhotos() {\n        let timeout: TimeInterval = 5\n        let hasContent = app.scrollViews.firstMatch.waitForExistence(timeout: timeout)\n            || app.collectionViews.firstMatch.waitForExistence(timeout: timeout)\n            || app.staticTexts.matching(NSPredicate(format: \"label CONTAINS '匯入'\")).firstMatch.waitForExistence(timeout: timeout)\n        XCTAssertTrue(hasContent, \"Gallery should show content or empty state\")\n    }\n}\n```\n\n### 步驟 3: 確認 UI Test target 配置\n\n- 如果無法新增 UITest target（因為需要 Xcode GUI），改為在 PrismTests 中加入 ViewInspector 風格的元件測試\n- 或者使用 XCTest 的 XCUIApplication 直接在 PrismTests 中（需要 host app）\n\n### 步驟 4: 跑測試\n\n```bash\nxcodebuild -project Prism.xcodeproj -scheme Prism -destination 'platform=iOS Simulator,name=iPhone 17 Pro' test\n```\n\n重要：\n- 先檢查 xcodeproj 是否已有 UITest target\n- 如果沒有，嘗試程式碼建立（修改 project.pbxproj）或改為在現有 test target 中測試\n- 所有測試必須通過\n- 模擬器上沒有真實相機，測試要考慮 placeholder",
  "touches": ["PrismUITests/LaunchTests.swift", "PrismUITests/CameraUITests.swift", "PrismUITests/GalleryUITests.swift"],
  "verify": "xcodebuild -project Prism.xcodeproj -scheme Prism -destination 'platform=iOS Simulator,name=iPhone 17 Pro' build 2>&1 | tail -3 | grep -q 'BUILD SUCCEEDED' && echo 'PASS'",
  "depends_on": ["apply-design-system"],
  "max_attempts": 8
}
